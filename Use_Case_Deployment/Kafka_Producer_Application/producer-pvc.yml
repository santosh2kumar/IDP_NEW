apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: producer-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: producersc

